[package]
name = "tracear-core"
version = "0.2.0"
edition = "2021"
license = "MIT"
description = "Portable computer-vision core for the Tracear web image tracking engine"

[dependencies]
nalgebra = { version = "0.33", default-features = false, features = ["std"] }
