/**
 * three.js adapter — lands in milestone M3 together with pose estimation.
 * (Until then the raw `update` event with the homography is the way to draw.)
 */
export function createThreeAnchor(): never {
  throw new Error("tracear/three: not implemented yet — arrives in milestone M3 (pose + filtering)");
}
